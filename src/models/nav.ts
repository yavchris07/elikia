export default class Chiffer{
    id:number;
    path:string;
    type:string;

    constructor(id:number,path:string,type:string){
        // eslint-disable-next-line @typescript-eslint/no-unused-expressions, no-sequences
        this.id=id,
        this.path =path,
        this.type=type
    }
}